{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Actus Liturgicus",
  "description": "A basic liturgical action or unit.",
  "type": "object",
  "properties": {
    "id": { "type": "string", "description": "A short unique identifier to be referenced." },
    "action": { "type": "string", "description": "Name of the liturgical action." }
  },
  "required": ["id", "action"],
  "additionalProperties": false
}
