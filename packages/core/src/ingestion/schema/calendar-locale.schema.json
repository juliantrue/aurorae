{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Calendar Locale",
  "description": "A locale scope for calendar data (universal, national, diocesan, etc.).",
  "type": "object",
  "properties": {
    "id": { "type": "string", "description": "A short unique identifier to be referenced." },
    "name": { "type": "string", "description": "Unique name of the calendar locale." },
    "scope": {
      "type": "string",
      "description": "Scope of the calendar locale.",
      "enum": ["UNIVERSAL", "NATIONAL", "DIOCESAN", "RELIGIOUS_ORDER", "LOCAL"]
    }
  },
  "required": ["id", "name", "scope"],
  "additionalProperties": false
}
