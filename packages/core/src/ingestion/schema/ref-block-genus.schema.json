{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ref Block Genus",
  "description": "A category for reference blocks (e.g., Psalm, Reading).",
  "type": "object",
  "properties": {
    "id": { "type": "string", "description": "A short unique identifier to be referenced." },
    "name": { "type": "string", "description": "Name of the reference block genus." },
    "description": { "type": "string", "description": "Description of the genus." }
  },
  "required": ["id", "name", "description"],
  "additionalProperties": false
}
