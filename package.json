{
  "name": "aurorae",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "init": "pnpm install && pnpm run prisma:generate && pnpm run prisma:db-push && pnpm run seed",
    "lint": "eslint",
    "test": "jest",
    "dev": "DATABASE_URL=file:./aurorae.db pnpm --filter @aurorae/aurorae run dev",
    "start": "DATABASE_URL=file:./aurorae.db pnpm --filter @aurorae/aurorae run start",
    "editor:dev": "pnpm --filter @aurorae/editor dev",
    "editor:build": "pnpm --filter @aurorae/editor build",
    "editor:lint": "pnpm --filter @aurorae/editor lint",
    "seed": "pnpm --filter @aurorae/database run seed",
    "clean": "git clean -Xdf",
    "prisma:generate": "pnpm --filter @aurorae/database run prisma:generate",
    "prisma:migrate": "pnpm --filter @aurorae/database run prisma:migrate",
    "prisma:studio": "pnpm --filter @aurorae/database run prisma:studio",
    "prisma:db-push": "pnpm --filter @aurorae/database run prisma:db-push",
    "vendor:init": "(git submodule status vendor/divinum-officium >/dev/null 2>&1 || git submodule add https://github.com/DivinumOfficium/divinum-officium.git vendor/divinum-officium) && git submodule update --init vendor/divinum-officium && git -C vendor/divinum-officium sparse-checkout init --cone && git -C vendor/divinum-officium sparse-checkout set web/www web/cgi-bin",
    "vendor:update": "git submodule update --remote --init vendor/divinum-officium && git -C vendor/divinum-officium sparse-checkout set web/www web/cgi-bin"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@swc/jest": "^0.2.39",
    "@types/cheerio": "^1.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^20",
    "cheerio": "^1.0.0",
    "diff": "^5.2.0",
    "eslint": "^9",
    "jest": "^30.2.0",
    "prettier": "^3.6.2",
    "prisma": "^6.17.0",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
